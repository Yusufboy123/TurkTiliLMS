import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { questionAnswersApi } from './question-answers.api';
import type { QuestionStatus } from './question-answers.types';
export function useStudentQuestions() { const client = useQueryClient(); const list = useQuery({ queryKey: ['student-questions'], queryFn: questionAnswersApi.studentList }); const create = useMutation({ mutationFn: questionAnswersApi.create, onSuccess: () => void client.invalidateQueries({ queryKey: ['student-questions'] }) }); return { list, create }; }
export function useStudentQuestion(id: string) { const client = useQueryClient(); const query = useQuery({ queryKey: ['student-question', id], queryFn: () => questionAnswersApi.studentDetail(id), enabled: Boolean(id) }); const send = useMutation({ mutationFn: (body: string) => questionAnswersApi.studentMessage(id, body), onSuccess: (data) => { client.setQueryData(['student-question', id], data); void client.invalidateQueries({ queryKey: ['student-questions'] }); } }); return { query, send }; }
export function useTeacherQuestions(status?: QuestionStatus) { const client = useQueryClient(); const list = useQuery({ queryKey: ['teacher-questions', status ?? 'all'], queryFn: () => questionAnswersApi.teacherList(status) }); const send = useMutation({ mutationFn: ({ id, body }: { id: string; body: string }) => questionAnswersApi.teacherMessage(id, body), onSuccess: () => void client.invalidateQueries({ queryKey: ['teacher-questions'] }) }); const close = useMutation({ mutationFn: ({ id, status: nextStatus }: { id: string; status: 'OPEN' | 'CLOSED' }) => questionAnswersApi.setStatus(id, nextStatus), onSuccess: () => void client.invalidateQueries({ queryKey: ['teacher-questions'] }) }); return { list, send, close }; }
export function useTeacherQuestion(id: string) { const client = useQueryClient(); const query = useQuery({ queryKey: ['teacher-question', id], queryFn: () => questionAnswersApi.teacherDetail(id), enabled: Boolean(id) }); const send = useMutation({ mutationFn: (body: string) => questionAnswersApi.teacherMessage(id, body), onSuccess: (data) => { client.setQueryData(['teacher-question', id], data); void client.invalidateQueries({ queryKey: ['teacher-questions'] }); } }); const close = useMutation({ mutationFn: (status: 'OPEN' | 'CLOSED') => questionAnswersApi.setStatus(id, status), onSuccess: (data) => { client.setQueryData(['teacher-question', id], data); void client.invalidateQueries({ queryKey: ['teacher-questions'] }); } }); return { query, send, close }; }
